# GrainBrokerApp
🌾 This app helps a grain broker manage and track grain orders by connecting customer demand with supplier fulfilment. It ingests order data from CSV, stores it in a database, and exposes clean APIs for an Angular dashboard to display order trends and history.
